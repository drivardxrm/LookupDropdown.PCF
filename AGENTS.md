## Agent skills

### Issue tracker

Issues tracked as GitHub Issues in `drivardxrm/LookupDropdown.PCF`, using the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout (root `CONTEXT.md` + `docs/adr/`). See `docs/agents/domain.md`.
